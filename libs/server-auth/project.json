{
  "name": "server-auth",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/server-auth/src",
  "projectType": "library",
  "tags": ["scope:backend"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/server-auth/jest.config.cts"
      }
    }
  }
}
